export default {
  noApiKey:
'npm-search: You need to provide apiKey as an env variable.',
  initialReplicationInProgress:
`npm-search: Only one instance of the replication can run at the same time.
Use reset=true env variable if necessary to restart the whole process`,
};
